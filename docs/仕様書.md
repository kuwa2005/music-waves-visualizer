# Music Waves Visualizer 仕様書

> **関連ドキュメント（本フォーク）**  
> ビルド手順・セキュリティ・FFmpeg 配信の詳細は [docs/README.md](./docs/README.md)（[BUILD](./docs/BUILD.md) / [SECURITY](./docs/SECURITY.md) / [FFMPEG](./docs/FFMPEG.md)）を参照してください。  
> **MP4 を音楽用と背景動画用に分ける挙動**は [docs/VIDEO_BACKGROUND.md](./docs/VIDEO_BACKGROUND.md) に集約（英日サマリ・Issue リンク）。

## 1. 概要

Music Waves Visualizerは、画像と音楽ファイルを読み込んで、音楽に合わせた音声波形を可視化した動画を作成するWebアプリケーションです。

### 1.1 主な機能

- **画像ファイルの読み込み**: 静止画ギャラリー、または **単体 MP4** を **背景動画**として利用（ルールは §3.1・[VIDEO_BACKGROUND.md](./docs/VIDEO_BACKGROUND.md)）
- **音楽ファイルの読み込み**: 音声波形の分析対象（**MP4 は音楽として** `HTMLVideoElement` 経由で抽出、または WAV 等を `AudioBuffer` で再生）
- **リアルタイム波形可視化**: 7つの表示モードで音声波形を可視化
- **解像度選択**: 手動（1920×1080、1080×1920、1920×1920）と自動（読み込んだ画像比率を16:9 / 9:16 / 1:1へ判定）
- **表示調整機能**: 各モードごとに倍率・位置を調整可能
- **動画長制限（ショート向け）**: 有効時は **開始（秒）** と **長さ（秒）** でプレビュー／録画の区間を指定。プリセットボタンは長さ欄への**目安値の入力のみ**（再生を自動で打ち切るものではない）。**長さを空欄**にすると、開始位置から**メディア末尾まで**再生する。
- **クライアント永続化**: 多くの設定は **第一党 Cookie**（`SameSite=Lax`）に保存し、旧版の **LocalStorage** にだけ残っている値は初回に Cookie へ移行する。**保存しない**: 読み込んだ画像・音楽の実体、**タイトル本文**、**SRT 字幕の本文**（字幕・タイトルのスタイルや表示 ON/OFF は保存対象に含まれる場合あり）。
- **プレビュー機能**: 音楽を再生しながら波形をリアルタイム表示
- **動画生成機能**: 音声と波形を動画として録画し、MP4形式で出力

### 1.2 デモサイト

https://music-waves-visualizer.vercel.app/

## 2. 技術スタック

### 2.1 フロントエンド

- **フレームワーク**: Next.js 13.5 系（`package.json` 参照）
- **言語**: TypeScript 4.6.2
- **UIライブラリ**: Material-UI (MUI) 5.11.4
- **スタイリング**: SCSS

### 2.2 主要ライブラリ

- **@ffmpeg/ffmpeg**: 0.10.1 - 動画変換処理（WebAssembly版）
- **@emotion/react/styled**: 11.10.5 - CSS-in-JS
- **@mui/icons-material**: 5.11.0 - アイコンコンポーネント

### 2.3 ブラウザAPI

- **Web Audio API**: 音声解析と再生
  - `AudioContext`
  - `AnalyserNode`
  - `AudioBuffer`
  - `MediaStreamAudioDestinationNode`
- **Canvas API**: 波形描画と画像表示
- **MediaRecorder API**: 動画録画
- **requestAnimationFrame**: アニメーション処理

## 3. 機能詳細

### 3.1 ファイル読み込み

#### 3.1.1 画像・背景（静止画ギャラリー / 単体 MP4 動画）

- **対応形式**
  - **静止画**: `image/*`（拡張子・MIME ゲートは `lib/fileValidation.ts` 参照）
  - **背景動画**: ファイル選択で **MP4 等を 1 本だけ**（同一ダイアログで静止画と **混在不可**）。**2 本以上の動画**を同時選択した場合はエラー。
- **「画像を選ぶ」（差し替え）**
  - 静止画のみ複数可（ギャラリー）。
  - **単体の動画 1 本のみ**選択した場合 → **背景を動画**に切替（静止画ギャラリーはクリア）。既に **2 枚以上**の静止画がある状態で MP4 を選んだ場合は **確認ダイアログ**の後にギャラリーを捨てて動画背景へ。
- **「画像を追加」**
  - **静止画のみ**。動画背景モード中は追加不可（スナックバーで案内）。
- **読み込み方法**: ドラッグ&ドロップ、または各ボタンから選択。
- **描画**: 静止画は従来どおり cover。動画背景は毎フレーム `drawVideoCover`（`lib/drawVideoCover.ts`）。音声タイムラインと **別ファイル**のときは `syncBackgroundOnlyVideo` で `currentTime` を同期。
- **詳細・Issue リンク**: [docs/VIDEO_BACKGROUND.md](./docs/VIDEO_BACKGROUND.md)

#### 3.1.2 音楽ファイル読み込み

- **対応形式**: `audio/*`、**MP4（映像＋音声トラックを `HTMLVideoElement` で再生し音声を解析）**
- **読み込み方法**:
  - ドラッグ&ドロップ: 静止画と **動画を同じドロップに混在**させた場合はエラー（そのドロップでは画像も動画音声も付けない）。**動画が複数**で静止画が無い場合もエラー。静止画のみ／音声のみ／単体動画（音楽扱い）などは従来の自動判定。
  - ボタンから選択: 「音楽ファイルを選ぶ」から選択
- **処理内容**:
  - 通常の音声ファイル: `ArrayBuffer` → `decodeAudioData()` → `AudioBuffer`。読み込み時に **別要素だった動画音声・スタンドアロン背景動画**を破棄して状態をリセットする。
  - MP4ファイル: `HTMLVideoElement` + `MediaElementAudioSourceNode` で音声を抽出。同一ファイルを背景にも使う場合は **二重デコードを避ける**実装（`reuseAudioVideoForBackground`）。
  - プレビュー・録画ボタンを有効化

#### 3.1.3 録画（MP4 固定）と品質設定

- **出力形式は MP4 固定**（アルファチャンネルは非対応）。
- 設定タブでは録画ビデオビットレートと MP4 音声（AAC）ビットレートを調整可能。
- 実装上は `MediaRecorder` で一時収録後、FFmpeg 経路で MP4 を生成する。

#### 3.1.4 レンダラーと既知制限

- **動画背景モード中は WebGL を選んでいても Canvas 2D にフォールバック**（背景に動画テクスチャを載せる WebGL 実装は Issue #36）。
- **音声と映像で別 MP4**の同期ドリフト低減は Issue #34。

### 3.2 波形可視化モード

内部では7つの表示モードがある。**画面上のボタンでは OFF に加え5種のみ選択可能**（周波数バー・円形・上下対称バー・ドット・グライコ風）。**モード1（折れ線）とモード5（波形・上下対称）はツールバーから非表示**だが描画ロジックは残している。`session_mode` が 1 または 5 で保存されていた場合は起動時にモード0へフォールバックする。

**スペクトラムアナライザーはプレビュー中・録画中のみ描画**され、エフェクトと同様に待機時は背景（静止画・動画背景、または単色／音声のみ動画時は透明クリア）のみ表示される。

#### モード0: 周波数バー（Frequency Bars）

- **データ取得**: `analyser.getByteFrequencyData()` - 周波数スペクトルデータ
- **表示方法**: 128本の縦バーを横に並べて表示
- **動作**: 各周波数帯域の強度をバーの高さで表現
- **色**: 白色（rgba(255, 255, 255, 0.8)）

#### モード1: 折れ線（Waveform）

- **UI**: 選択ボタンは非表示（上記参照）

- **データ取得**: `analyser.getByteTimeDomainData()` - 波形データ
- **表示方法**: 時系列データを折れ線グラフで表示
- **動作**: 音声の実際の波形をリアルタイムで描画
- **色**: 白色のストローク（rgba(255, 255, 255, 0.8)）

#### モード2: 円形（Circular）

- **データ取得**: `analyser.getByteFrequencyData()` - 周波数スペクトルデータ
- **表示方法**: 円形に配置された256本のバー
- **動作**: 
  - 低音（1Hz）の強度に基づいて中心からの距離を調整
  - 各周波数を円周上に配置して回転表示
- **色**: 白色（rgba(255, 255, 255, 0.8)）

#### モード3: 上下対称バー（Symmetrical Bars）

- **データ取得**: `analyser.getByteFrequencyData()` - 周波数スペクトルデータ
- **表示方法**: 中央から上下に広がる128本のカラフルなバー
- **動作**: 
  - 各周波数帯域の強度をバーの高さで表現
  - 中央線（canvasHeight / 2）を基準に上下対称に描画
  - 周波数に応じたHSL色空間で色分け（0度から360度のグラデーション）
- **色**: 周波数に応じたグラデーション（`hsla(hue, 100%, 50-70%, 0.8)`）

#### モード4: ドット表示（Dot Display）

- **データ取得**: `analyser.getByteFrequencyData()` - 周波数スペクトルデータ
- **表示方法**: 32列×16行のドットマトリクス表示（合計512ドット）
- **動作**: 
  - 周波数データを32列にマッピング
  - 各列の強度に応じて16行のドットを縦に並べて表示
  - 強度が閾値を超えるとドットが表示される
- **色**: 列ごとに色相が変化（HSL色空間、`hsla(hue, 100%, 50%, opacity)`）

#### モード5: 波形（上下対称）（Symmetrical Waveform）

- **UI**: 選択ボタンは非表示（上記参照）

- **データ取得**: `analyser.getByteTimeDomainData()` - 波形データ
- **表示方法**: 波形を上下対称に表示
- **動作**: 
  - 中央線を基準に波形を描画
  - 同じ波形を上下反転して描画（scale(1, -1)で反転）
  - 音声の実際の波形を上下対称に可視化
- **色**: 白色のストローク（rgba(255, 255, 255, 0.8)、線幅2px）

#### モード6: グライコ風（Glyco Style）

- **データ取得**: `analyser.getByteFrequencyData()` - 周波数スペクトルデータ
- **表示方法**: 64本のバーを1980年代コンポ風のピークホールド表示（帯域左右いっぱい）
- **動作**: 
  - 各バーは現在の周波数強度を表示
  - ピークレベルを「-」のダッシュでホールド（約350ms保持後、ゆっくり減衰）
  - 1980年代のシンセ・コンポのVUメーター風の挙動
  - 縦倍率3.0＝従来の1.0相当、横倍率1.0＝画面横幅いっぱい
- **色**: 表示・音量設定で選択可能（単色8種＋ネオン3種＋グラデーション3種）

### 3.3 Canvasサイズ選択

解像度は手動3種＋自動モードを選択可能：

- **自動**: 読み込んだ画像の縦横比から 16:9 / 9:16 / 1:1 のいずれかを自動判定して適用
- **横長 1920×1080 (16:9)**: 一般的な動画サイズ（横長）
- **縦長 1080×1920 (9:16)**: 縦型動画用（縦長、SNS対応）
- **正方形 1920×1920 (1:1)**: 正方形動画用（Instagram投稿など）

各解像度に対応してCanvasサイズが動的に変更されます。選択した解像度（自動時は判定結果）が録画時の実際の動画サイズとなります（プレビュー表示サイズとは無関係）。
**自動モード**は「自動で選ばれたレイアウト（16:9 / 9:16 / 1:1）」として Cookie に保存され、次回起動時に復元される。手動で横長・縦長・正方形を選んだ場合も同様に保存される。

### 3.4 表示・音量設定

#### 3.4.1 音量設定（目標LUFS）【共通設定】

動画サイトの推奨値に合わせて音量を正規化。MP4変換時にFFmpegのloudnormフィルターを適用。

- **なし**: 正規化しない
- **YouTube等 (-14 LUFS)**: YouTube、多数の動画サイト向け
- **ニコニコ動画 (-15 LUFS)**: ニコニコ動画向け
- **任意の値**: -60〜0 dBの範囲で手入力可能

**なし（正規化オフ）**も明示的に保存され、エクスポート/インポート対象です。

#### 3.4.2 表示調整機能【レイアウト別】

縦画面（1080×1920）、横画面（1920×1080）、正方形（1920×1920）ごとに、各スペアナモードの倍率・位置を調整可能：

- **横幅倍率（scaleX）**: 0.1〜3.0（デフォルト: 1.0）
- **縦幅倍率（scaleY）**: 0.1〜3.0（デフォルト: 1.0）
- **横位置（offsetX）**: -150%〜150%（Canvasサイズに対するパーセンテージ、デフォルト: 0%）
- **縦位置（offsetY）**: -150%〜150%（Canvasサイズに対するパーセンテージ、デフォルト: 0%）

レイアウト×スペアナモードごとに自動保存され、エクスポート/インポート対象です。

### 3.5 エフェクト機能【共通設定】

スペクトラムとは独立してON/OFF可能なオーバーレイエフェクト。プレビューまたは動画生成中のみアニメーション。**各エフェクトごとに強度（弱・中・強）を個別に保存**し、共通設定としてエクスポート/インポート対象です。

- **宇宙空間**: パラメータパネルでタイプ1/2/3（通常・等速・音源連動）を選択可能
- **ビネット**: 画面端を暗くし中央に視線を集める（音量で強度変化）
- **レインボー**: 色相が時間で変化するオーバーレイ（音量で速度変化）
- **カーテン**: 波状のカーテンが流れる演出（ベースで波の速度・振幅が変化）
- **きらきら**: 白の＋/X/＊形が点滅。**ラジアルグロウ（外側へのぼかし拡散）は使わず**、星形ストロークと中心の芯のみ。Canvas 2D と WebGL で見た目方針を揃える。
- **空気感（ほこり）**: 低コントラストの微粒子。大きい粒は出現しにくい分布。Canvas 2D は単色の円＋`lighter`、WebGL は加算ブレンドの円（いずれも大きなソフトグロウの円は使わない）。
- **雨 / 雪**: 降り注ぐ角度・量・色を調整可能

### 3.6 プレビュー機能

- **動作**:
  1. `AudioBufferSourceNode`または`HTMLVideoElement`から音声を取得
  2. `AnalyserNode`とスピーカー出力、`MediaStreamAudioDestinationNode`に接続
  3. 音声再生を開始
  4. `requestAnimationFrame`でリアルタイム波形描画（調整パラメータを適用）
- **停止**: 再生中に再度ボタンをクリックすると停止。曲終了時は自動停止

### 3.7 動画生成機能

- **録画プロセス**:
  1. Canvasのストリームと音声ストリームを結合
  2. `MediaRecorder`でWebM形式（H.264コーデック）で録画開始
  3. 音楽再生を自動開始
  4. 音楽終了時に録画停止
- **変換プロセス**:
  1. 録画されたWebMデータをBlobとして取得
  2. FFmpeg（WebAssembly版）でMP4形式に変換
  3. 変換完了後、自動ダウンロード
- **ファイル名**: `movie_[ランダム8文字].mp4`
- **録画サイズ**: 選択したCanvasサイズ（プレビュー表示サイズとは無関係）

### 3.8 UIコンポーネント

#### ボタン

- **画像ファイルを選ぶ**: 画像ファイル選択ダイアログを開く（ドロップゾーン内に配置）
- **音楽ファイルを選ぶ**: 音声ファイル選択ダイアログを開く（ドロップゾーン内に配置）
- **スペクトラムアナライザー選択**: 7つのモードからボタンで選択
  - 周波数バー、折れ線、円形、上下対称バー、ドット表示、波形（上下対称）、グライコ風
- **解像度選択**: 3つの解像度からボタンで選択
  - 「横長 1920×1080 (16:9)」「縦長 1080×1920 (9:16)」「正方形 1920×1920 (1:1)」
- **プレビュー**: 音楽再生と波形表示の開始/停止（音楽読み込み後有効化）
- **動画を生成**: 録画開始（音楽読み込み後、再生中以外で有効化）

#### 表示・音量設定UI（アコーディオン）

- **音量設定**: 目標LUFS（なし/YouTube等-14/ニコニコ-15/任意値）
- **横幅倍率スライダー**: 0.1〜3.0
- **縦幅倍率スライダー**: 0.1〜3.0
- **横位置スライダー**: -150%〜150%（パーセンテージと実ピクセル値を表示）
- **縦位置スライダー**: -150%〜150%（パーセンテージと実ピクセル値を表示）

#### 設定管理UI

- **エクスポート**: 全設定を一括でJSON形式でダウンロード（共通設定＋全レイアウト×全スペアナ）
- **インポート**: ファイル選択またはJSON貼り付けで、存在する項目のみ上書き（旧形式にも対応）
- **クリア**: すべての保存された設定を削除

#### 通知（Snackbar）

- **表示タイミング**:
  - 画像読み込み完了
  - 音楽読み込み完了
  - 録画開始
  - MP4変換中
  - 変換完了
  - エラー発生時

#### ファイル名表示

- **画像ファイル名**: 「画像ファイルを選ぶ」ボタンの横に表示
- **音楽ファイル名**: 「音楽ファイルを選ぶ」ボタンの横に表示

## 4. アーキテクチャ

### 4.1 ファイル構成

```
music-waves-visualizer/
├── pages/
│   ├── _app.tsx          # アプリケーション全体設定（メタタグ等）
│   └── index.tsx          # メインページ（UIと状態管理）
├── components/
│   └── CustomSnackbar.tsx # 通知コンポーネント
├── lib/
│   ├── Canvas.ts          # Canvas描画ロジック
│   └── Ffmpeg.ts          # FFmpeg動画変換処理
├── styles/
│   ├── globals.scss       # グローバルスタイル
│   └── Home.module.scss   # ホームページスタイル
└── public/                # 静的ファイル
```

### 4.2 主要な処理フロー

#### 4.2.1 初期化

1. `AudioContext`の作成
2. `AnalyserNode`の設定（FFTサイズ: 2048）
3. `MediaStreamAudioDestinationNode`の作成（録画用）
4. Canvasの初期化

#### 4.2.2 画像読み込みフロー

```
ファイル選択 → Imageオブジェクト作成 → onload → Canvas描画開始
```

#### 4.2.3 音楽読み込みフロー

```
ファイル選択 → ArrayBuffer読み込み → decodeAudioData → AudioBuffer保存 → ボタン有効化
```

#### 4.2.4 プレビューフロー

```
ボタンクリック → AudioBufferSourceNode作成 → 接続 → 再生開始 → アニメーションループ開始
```

#### 4.2.5 録画フロー

```
ボタンクリック → ストリーム結合 → MediaRecorder作成 → 録画開始 → 音楽再生 → 
音楽終了 → 録画停止 → WebM → FFmpeg → MP4 → ダウンロード
```

### 4.3 状態管理

React Hooksを使用したローカル状態管理：

- `useState`: UI状態（再生中、ボタン有効/無効、モード選択、通知）
- `useRef`: 永続的な参照（AudioContext、Canvas、AudioBuffer、ノード参照）
- `useEffect`: 副作用処理（初期化、アニメーションループ）

## 5. 技術的な詳細

### 5.1 Canvas設定

- **サイズ**: 1920×1080、1080×1920、1920×1920から選択（動的に変更可能）
- **背景色**: 黒色（rgba(34, 34, 34, 1.0)）
- **更新頻度**: `requestAnimationFrame`による60fps相当
- **GPU加速**: `canvas.getContext("2d", { alpha: false, desynchronized: true, willReadFrequently: false })`で有効化
- **プレビュー表示**: CSSで最大480px幅（9:16の場合は240px）に縮小表示（録画サイズには影響なし）

### 5.2 Audio API設定

- **FFTサイズ**: 2048（周波数バッファ: 1024）
- **サンプリングレート**: 音声ファイルに依存
- **接続構成**:
  ```
  AudioBufferSourceNode
    ├── AnalyserNode (波形分析)
    ├── AudioDestinationNode (スピーカー出力)
    └── MediaStreamAudioDestinationNode (録画用)
  ```

### 5.3 録画設定

- **録画形式**: WebM（H.264コーデック）
- **変換形式**: MP4
- **FFmpegコマンド**: `-i input.webm -vcodec copy output.mp4`
- **FFmpegバージョン**: 0.10.0（WebAssembly版）

### 5.4 セキュリティ設定

`next.config.js`で以下のHTTPヘッダーを設定：

- `Cross-Origin-Opener-Policy: same-origin`
- `Cross-Origin-Embedder-Policy: require-corp`

これにより、SharedArrayBufferなどの機能を使用可能にします。

### 5.5 ブラウザ互換性

- **対応ブラウザ**: Chrome、Firefox、Edge（最新版）
- **未確認**: iOS Safari、Android Chrome
- **注意事項**: 
  - 離脱ガード機能あり（`beforeunload`イベント）
  - ブラウザのプレフィックス対応（`webkit`、`moz`）

## 6. 制限事項・注意事項

### 6.1 制限事項

1. **モバイル対応**: iOS、Androidは動作未確認
2. **ファイルサイズ**: 大きな音楽ファイルや画像ファイルは処理に時間がかかる可能性
3. **メモリ**: 長時間の音楽や高解像度画像はメモリ消費が大きい
4. **録画時間**: 音楽の長さに依存（録画時間 = 音楽の長さ）

### 6.2 注意事項

1. **録画中の操作**: 録画中は他の操作を控えること
2. **ブラウザタブ**: 録画中はブラウザタブを閉じないこと（離脱ガードあり）
3. **変換時間**: MP4変換には時間がかかる場合がある（ブラウザコンソールでログ確認可能）

## 7. 今後の拡張可能性

- 波形の色・スタイルカスタマイズ
- 追加の可視化モード
- 動画品質設定（解像度、ビットレート）
- 複数画像の切り替え機能
- エフェクト機能（フィルター、アニメーション）

## 8. 設定の保存・エクスポート/インポート

### 保存先（ブラウザ）

- **第一党 Cookie**: 解像度プリセット、クリップ長、ギャラリー自動再生間隔、エフェクト・音量・スペアナ調整など多くの UI 設定。値が大きい場合は複数 Cookie に分割して保存する。
- **LocalStorage**: 旧バージョン由来のキーが残っている場合のみ読み取り、移行後は削除されることがある（実装依存）。

### 保存対象（概要）

- **共通設定**: 音量調整（目標LUFS、**なし**含む）、エフェクト種類、各エフェクトの強度 など
- **レイアウト別**: 確定したレイアウト（16:9 / 9:16 / 1:1）ごとの各スペアナモードの倍率・位置
- **保存しない（原則）**: メディアファイル本体、タイトル用の自由記述、SRT の本文（詳細は [PRIVACY_POLICY.md](./PRIVACY_POLICY.md)）

### エクスポート/インポートの動作

- **エクスポート**: 全設定を一括でJSON出力（全レイアウト×全スペアナを含む）
- **インポート**: 存在する項目のみ上書き（ファイルに含まれる項目だけを更新、他は維持）

### エクスポート形式

```json
{
  "common": {
    "targetLufs": -14,
    "effectType": "space",
    "effectDensities": { "space": 2, "vignette": 1, ... }
  },
  "spectrumSettings": {
    "1920x1080": { "0": { "scaleX": 1, "scaleY": 1, "offsetX": 0, "offsetY": 0 }, ... },
    "1080x1920": { ... },
    "1920x1920": { ... }
  }
}
```

## 9. 開発者モード

環境変数 `NEXT_PUBLIC_DEVELOPER_MODE=true` を設定すると、FPS表示などの開発用機能が有効になります。設定の保存・エクスポート/インポートは開発者モードなしでも利用可能です。

詳細は [DEVELOPER_MODE.md](./DEVELOPER_MODE.md) を参照してください。

## 10. 開発者情報・ライセンス

- **ライセンス**: MIT License（[LICENSE](./LICENSE) 参照）
- **元作者**: komura-c
- **改変版作者**: KURAGASHI (kuwa2005)
- **元リポジトリ**: https://github.com/komura-c/music-waves-visualizer
- **改変版リポジトリ**: https://github.com/kuwa2005/music-waves-visualizer
- **元記事**: [オーディオ波形動画を生成するWebぺージの作り方](https://tech-blog.voicy.jp/entry/2022/12/11/235929)
- **依存ライブラリのライセンス**: [NOTICE](./NOTICE) を参照

